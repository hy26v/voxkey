[package]
name = "voxkey-ipc"
version = "0.4.0"
edition = "2024"
license = "MIT"

[dependencies]
zbus = { version = "5", default-features = false, features = ["tokio"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
toml = "0.8"
